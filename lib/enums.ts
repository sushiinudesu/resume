export enum ProjectRole {
  Frontend = "frontend",
  Backend = "backend",
  Fullstack = "fullstack",
}

export enum CompanyRole {
  Junior = "junior",
  Middle = "middle",
  Senior = "senior",
}

export enum CompanyLocation {
  Remote = "remote",
  Onsite = "onsite",
}

export enum Locales {
  English = "en",
  Russian = "ru",
}